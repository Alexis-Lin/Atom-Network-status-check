# 开发注意事项 · 业务逻辑清单

> 本文是 [`PRD-atom网络状态检测与信号同步.md`](./PRD-atom网络状态检测与信号同步.md) 的**开发侧提炼**：把散落在各章的约束、互斥、边界规则收成一张可勾选的清单。与 PRD 不一致时以 PRD 为准。配套代码：[`参考代码-网络检测状态机.c`](./参考代码-网络检测状态机.c)（设备端）、[`参考代码-后端服务.py`](./参考代码-后端服务.py)（服务端）。

## 1. 检测调度（PRD B1 / B2）

- [ ] **课中互斥**：上课期间禁止发起完整测速（入口置灰 +「课程中暂不可测速」），常驻轻探测也停止——课中灯色唯一数据源是 TRTC `onNetworkQuality`；
- [ ] **防重入**：同一时间仅一个测速任务（官方限制），按钮点击需防抖；
- [ ] **硬超时 40s**：超时调 `stopSpeedTest` 进「检测超时」失败态；已回传的半程样本仍随上报带出；
- [ ] **息屏不采样**：熄屏 / 休眠停止一切探测（续航优先）；唤醒 / 亮屏立即补测一次；
- [ ] **按 SSID 缓存**：灯色、样本、weak_streak 全部按 SSID 隔离；**切换 WiFi 立即清缓存重测，灯色绝不跨网继承**；
- [ ] **5 分钟时效**：样本超过 `freshness_window`（默认 300s，OTA 可调）不外显旧灯色，显示「检测中…」并触发刷新；
- [ ] **3 样本防抖**：连续 3 个同级样本才切灯（课中为 3×2s 回调 ≈ 6s）；
- [ ] **30s 静默刷新**：WiFi 页后台刷新原地更新，**不显示任何加载态**（避免半分钟闪一次）。

## 2. 测速实现（PRD B3 / A4.7）

- [ ] **TRTC 课外可用性待实测**（蔡博）——`startSpeedTest` 官方限制进房前调用，若课外场景不可用，切换**备选文件测速**（对自家服务器上传/下载测试文件，接口见后端参考代码；UI / 状态机完全复用）；
- [ ] **时长非固定**：节点数 `totalCount` 由服务端下发，典型 10–30s；**进度环按 `finishedCount/totalCount` 驱动，绝不按时间**；
- [ ] **测速产生计费流量**（TRTC 按正常音视频计费）+ 用户真实带宽消耗，不做自动周期性完整测速；
- [ ] **-1 无效值**：`availableUp/DownBandwidth = -1` → 显示「—」、不着色、不入波形；带宽值在延迟阶段无效，波形从首个有效样本画起；
- [ ] **波形数据 = 真实回调**：不插值、不平滑、不做假动画；结果页复用同一份数据（不重绘不重采样）；
- [ ] **测速中断网 / 切网**：立即取消任务进失败态并注明原因。

## 3. 显示规则（PRD A4）

- [ ] **指标独立着色 vs 整体灯色**：四指标各按自己阈值着色（`metric_color_*`），整页灯色以 TRTC `quality` 为准——**两者允许不一致**，不要「对齐」它们；
- [ ] **颜色只出现在三处**：结论标题（灯色）、超标指标、bars 灯色；按钮无彩色、波形禁红黄绿、影响/建议白灰；
- [ ] **失败态不给灯色**（没测出来 ≠ 网络差）；`errMsg` 仅上报不上屏；
- [ ] **按钮槽位固定**：左槽恒「重测」（描边），右槽恒「OK / 前进」（实底）；四态位置一致，重测循环中按钮不位移；
- [ ] **超长 SSID**：日期时间永不截断，SSID 弹性尾部省略号；隐藏 SSID 显示「已连接网络」；
- [ ] **数字格式**：等宽数字（tabular figures）；延迟封顶「999+」、丢包封顶 100%、带宽 <10M 一位小数 / ≥10M 取整；
- [ ] **时钟未同步**：表头显示「时间未同步」，上报用相对时间戳。

## 4. 通知条目（PRD A3，单一网络位）

- [ ] **通知栏网络信息只有一个条目**：事件不新增卡片，只升级条目内容（优先级：长期建议 > 短期弱网 > 实时状态）；
- [ ] **升级触发**：短期 = 同 WiFi 连续 3 次黄/红（每 WiFi 每天最多一次）；长期 = 连续 3 天课中红灯（7 天冷却）；
- [ ] **回落规则**：点击即回落（并进小程序）；短期态恢复绿灯自动回落；长期态未点击 7 天自动回落；
- [ ] **绿灯永不升级**（绿灯静默约束主动打扰）；**课中不升级**（课中提醒归模块 C）；
- [ ] 磁贴 bars **三态常显**（含绿）——常驻静态显示不算打扰。

## 5. 课中（详见 [`PRD-3-课中双端提示.md`](./PRD-3-课中双端提示.md)，含现状代码函数级改造映射）

- [ ] toast 冷却 5 分钟、每节课最多 2 次、自动消失、零操作；方向归因（上行差=你的画面 / 下行差=课程画面）；
- [ ] **带宽保护**：动作识别期间暂停后台上传；重进课程取消未完成的上传队列（v1 不做断点续传）；
- [ ] 全屏中断页是**唯一**允许的阻断样式（仅真断线且自动重连失败）；「重试 / OK」类手动确认弹窗全部移除。

## 6. 上报与配置（PRD B5）

- [ ] **批量 + 离线补传**：样本本地环形缓存，网络恢复后补传；单条失败不阻塞；
- [ ] **阈值 OTA**：`net_config_t` 全字段可远程下发（版本号比对，见后端 `/v1/net/config`）；
- [ ] **隐私**：SSID 上报前做哈希或脱敏处理（客服时间线用哈希关联即可，不需要明文）；
- [ ] 测速结果本地存近 10 次（按 SSID 分组）。

## 7. 待确认（阻塞项）

| 事项 | Owner | 影响 |
| --- | --- | --- |
| TRTC 课外测速可用性实测 | 蔡博 | 决定 B3 主路径 or 备选文件测速 |
| PC 教练端文件测速实现细节 | 张恒 | 备选路径的服务端对齐 |
| 存量课中弹窗完整清单 | 客户端 | C2 保留/降级/删除映射表 |
| 待机整点采样功耗实测 | 硬件 | 60 vs 120 分钟间隔取值 |
