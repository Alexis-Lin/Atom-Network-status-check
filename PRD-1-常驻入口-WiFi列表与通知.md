# PRD-1 · 常驻入口：WiFi 列表与通知

| 项 | 内容 |
| --- | --- |
| 版本 | V1.1（自总览 PRD V1.1 拆出，内容一致） |
| 范围 | WiFi 列表页 · 控制中心磁贴 · 上拉通知栏（原模块 A 的 A1–A3） |
| 前置阅读 | [`PRD-atom网络状态检测与信号同步.md`](./PRD-atom网络状态检测与信号同步.md)（背景 / 三态定义 / 检测逻辑模块 B / 文案总表） |

> 各入口共享模块 B 的同一状态机与数据，任何入口灯色一致。可演示版见 [`Demo-界面交互UI设计.html`](./Demo-界面交互UI设计.html)。

![常驻入口：通知栏 / WiFi 列表页三态 / 控制中心 / 通知条目升级态](./images/界面-常驻入口.png)

## A1 WiFi 列表页（原 F2.2–F2.7）

- **A1.1 状态行锚定已连接网络**：已连接网络自动置顶到 WiFi 开关正下方（iOS 式动态排序），**网速状态行紧贴其下**——灯挂在它描述的网络上，位置即语义（与手机端「灯随用户画面」同一原则）；其余曾配网络归入分隔线下「其他曾配网络」分组。样式：三格 bars（灯色）+ 网速状态 + 弱化灰色「测速 ›」，无卡片底。文案三态：**网速良好 / 网速一般 / 网速较慢**；「测速 ›」三态恒显（本页唯一测速入口，恒显保证可发现性）。位置三方案对比见 [`backup/决策参考与迭代过程.html`](./backup/决策参考与迭代过程.html) Part 1；
- **A1.2 热区**：状态行整行为点击热区（高度 ≥ 60px），点击进入网速测试小程序（A4）；
- **A1.3 状态联动**：WiFi 关闭或未连接 → 无已连接行，状态行不显示（未连接由列表自身表达）；连接中 → 在对应网络行内显示「连接中…」；已连接但暂无有效数据（首次进屏 / 刚连上 / 缓存过期）→ 三格全暗 +「检测中…」；有数据 → 三色。**后台 30 秒静默刷新原地更新，不显示任何「刷新中」加载态**（避免每半分钟闪一次）；
- **A1.4 连接流程系统逻辑（开发对接）**：(a) 开启 WiFi → 扫描状态；(b) 扫描完成 → 自动从「曾配对网络」择优连接；(c) 连接成功 → 列表轻刷新动效：已连接网络上浮置顶，其下方跟上网速状态行（先「检测中…」后三色）；
- **A1.5 头部文字（开发对接）**：图标下方无效的「WiFi」标题改为**「常见网络问题」**，整行可点击 → 连接帮助 tips 页。

## A2 WiFi 快捷看板（控制中心磁贴，原 F2.1 / F2.5）

- **A2.1** 磁贴内 WiFi 图标右侧**三态常显**小型三格 bars（3 格绿 / 2 格黄 / 1 格红）。**决策记录**：原方案绿灯隐藏 bars、图标回中（依据「绿灯静默」），评审后改为常显——①「绿灯静默」约束的是**主动打扰**（通知 / toast / 弹窗），常驻区的静态显示不构成打扰；②常显让用户对「这里能看网速质量」建立稳定预期，绿灯时 bars 消失反而会被疑为「没在检测 / 功能坏了」；③与通知栏常驻条目（绿灯也显示「网速良好」）行为一致。**字形决策**：不用图标弧格填充度表达质量（弧格 = 信号强度语义，「信号满格但宽带拥塞」时必然误导）；不用图标染色（与开关语义打架）；不用角标圆点（与全系统 bars 不一致 + 17px 过小 + 录制红点歧义）；
- **A2.2** 系统状态栏若展示 WiFi 图标，黄 / 红时同步染色（与磁贴同一状态机）。

## A3 上拉通知栏（原 F1 + F7.5 / F5.3 通知）

- **A3.1 唯一网络位**：通知栏里网络信息**有且只有一个条目**——顶部常驻、位于所有通知之上、不可清除；bars + 标题 + 副行，任何内容态下点击都进入小程序（A4）；展开通知栏时立即触发一次刷新；
- **A3.2** 课中不弹通知栏提醒（课中提醒由模块 C 负责，避免双重打扰）；
- **A3.3 条目内容按优先级升级（取代原「两级弱网推送」）**：

| 优先级 | 触发 | 条目显示 | 回落 |
| --- | --- | --- | --- |
| 1 · 长期建议 | 连续 3 天课中红灯累计超阈值 | 「上课网络较弱 / 建议靠近路由器」 | 点击进小程序后回落；未点击 7 天自动回落；7 天内不重复升级 |
| 2 · 短期弱网 | 同一 WiFi 连续 3 次采样黄 / 红 | 「当前 WiFi 网速较弱 / 可能影响上课」 | 恢复绿灯或点击后回落；每 WiFi 每天最多升级一次 |
| 3 · 实时状态（默认） | 常驻 | 绿「网速良好」/ 黄「网速一般 · 点击测速」/ 红「网速较慢 · 点击查看建议」 | — |

  **决策记录**：原方案 = 常驻条目 + 独立推送卡片并存，被否——同一面板可能同时出现两条网络信息，语义重复；且推送卡是历史快照、条目是实时状态，两者可能互相矛盾（卡片说「较弱」、条目已变绿）。合并后**通知栏的网络信息只有一个真相源**，事件不新增条目、只升级既有条目的内容；绿灯时不发生任何升级（绿灯静默约束主动打扰）。

