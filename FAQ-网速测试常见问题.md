# FAQ · 网速测试常见问题解答

> 触达方式：在网速测试小程序内**长按非按钮区域的背景（约 0.8 秒）**弹出本说明（交互规格见 [PRD-2 A4.9](./PRD-2-网速测试小程序.md)）。本文含两个版本：**完整版**（客服 / 官网 / 手机 App 帮助页用）与**屏上精简版**（466 圆屏逐条文案，字数已裁）。

---

## 完整版（6 问）

### Q1 为什么 ATOM 还需要自己的网络测试？

ATOM 上课和「看视频」不一样：它要把**摄像头画面实时上传**到课程服务器做 AI 动作识别与计数，同时把课程内容传回来。上课体验取决于这条「**设备 ⇄ 课程服务器**」的专用链路——而这条链路好不好，路由器上的信号格数、宽带合同上的兆数都看不出来。内置测试测的就是这条**真正用来上课的链路**。

### Q2 这个测速和 Speedtest / 运营商测速有什么不同？

手机测速工具回答的是「**你家宽带到最近测速节点的极限速度有多快**」，数字通常很大；ATOM 的测试回答的是「**这台设备到课程服务器，按上课实际需要的带宽，够不够、稳不稳**」（上行约 1.5 Mbps / 下行约 2 Mbps）。所以两边结果对不上是正常的：宽带 300M 也可能在这里显示黄灯——瓶颈通常不在宽带，而在 WiFi 信号、路由器负载或跨网链路。**一句话：别人测「有多快」，我们测「上课稳不稳」。**

### Q3 上行 / 下行是什么意思？跟我有什么关系？

- **上行** = 你的画面传出去（AI 动作识别、计数、教练看你）——**上行差，你的画面会卡、识别和计数可能延迟**；
- **下行** = 课程画面传回来——**下行差，课程画面会卡**。

ATOM 是少见的「上行大户」（多数家用设备只重下行），这也是它比普通设备更挑网络的原因。报告里两条曲线和两个指标分开给，就是为了让你一眼看出卡在哪个方向。

### Q4 显示「网速良好」，为什么上课还是会卡？

测速是**那一刻的采样**，而网络是波动的：家人同时下载、投屏、微波炉、隔墙，都可能让 WiFi 瞬间变差。所以课中我们**持续监测**——一旦变差会在屏幕底部提示，并自动降低画质、自动重连，不需要你操作。如果某个 WiFi 反复出现红灯（通知栏会汇总提醒），建议换 5GHz 频段、靠近路由器或更换网络。

### Q5 测速会消耗流量、影响家里网络吗？

会占用**少量带宽**（一次约数十 MB 量级，取决于时长），且**只在你主动点「测速」时发生**，测完即停，绝不后台偷跑。上课期间测速入口自动置灰——测速本身占带宽，不能挤占课程。

### Q6 报告里的数字和曲线是怎么来的？可以用来报障吗？

可以，这正是报告卡的设计目的。曲线上**每个点都是一次真实回传的样本**（不插值、不美化），间距不均匀说明那段时间回传变慢——这本身就是信息；延迟 / 丢包 / 上下行四个数字是整次测试结束后的**汇总结论值**。**整张报告卡截图发给客服**，就能与后台日志对上，无需口述。

---

## 屏上精简版（圆屏逐条文案）

> 规格：问题行 24px 白加粗、答案 24px 灰；每问答案 ≤ 3 行（每行 ≤ 15 全角）。EN 供中英切换。

| # | 问题（CN / EN） | 答案（CN） | 答案（EN） |
| --- | --- | --- | --- |
| 1 | 为什么要自带网络测试？/ Why a built-in test? | 上课要把你的画面实时传到课程服务器做动作识别；这条链路好不好，路由器信号格看不出来。 | Classes stream your camera for AI tracking — router bars can't show that link. |
| 2 | 和手机测速软件有何不同？/ Vs. phone speed tests? | 手机测的是宽带极限速度；这里测「到课程服务器、按上课所需，够不够稳」。结果对不上是正常的。 | Those test broadband peak; this checks the class link. |
| 3 | 上行 / 下行是什么？/ What are Up / Down? | 上行 = 你的画面传出去，差了识别计数会延迟；下行 = 课程画面传回来，差了课程会卡。 | Up = your camera feed out (AI tracking); Down = class video in. |
| 4 | 「网速良好」为何还会卡？/ Good result but still lags? | 测速是当下采样，网络会波动；课中持续监测，变差会底部提示并自动降画质、自动重连。 | A test is one moment; we keep watching in class and auto-adapt when it dips. |
| 5 | 测速费流量吗？/ Does testing cost data? | 占少量带宽，只在你点「测速」时发生，测完即停；课程中入口自动置灰。 | A little, only when you tap Test; never during class. |
| 6 | 数字和曲线怎么来的？/ Where do the numbers come from? | 每个点都是真实回传样本，四个数字是整次汇总；整卡截图发客服即可对上后台日志。 | Every dot is a real sample; screenshot the card for support. |

---

## 入口与交互（摘要，规格详见 PRD-2 A4.9）

- **触发**：小程序任意页面，长按背景（非按钮 / 非报告卡交互区）**约 0.8s** + 轻震动 → 全屏 FAQ 页；
- **为什么用隐藏入口**：说明属低频需求，不值得占常驻 UI；困惑发生时（失败态 / 首次使用）再引导——**首次进入小程序在底部一次性提示「长按空白处可查看说明」**；
- **FAQ 页形态**：标题「关于网速测试」（32px）+ 可滚动 Q&A 列表（问题 24 白粗 / 答案 24 灰）；下滑或点任意空白处关闭，返回原页面且状态保留（测速任务不中断）；
- 文案变更走本文件，与 PRD 文案表同源管理；上屏字数按精简版表执行。
