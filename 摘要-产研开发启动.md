# ATOM 网络状态「红绿灯」· 产研开发启动摘要

> 一页纸版本，面向产研全员。细节以各 PRD 为准；本文只回答「做什么、为什么、从哪开始」。
> 设计状态：**全套定稿**（总览 V1.2 / PRD-1 V1.1 / PRD-2 V1.2 / PRD-3 V1.0）。

## 1. 解决什么问题

用户连上 WiFi 但上课仍卡顿——设备只显示「已连接」，用户**无法感知网速、无法归因、还被课中弹窗打断**。本方案 = 一套红绿灯状态机 + 一个网速测试小程序 + 课中提示收敛，覆盖 ATOM 设备端与手机 App。

**四条不动摇的原则**：① 位置即语义（灯挂在它描述的对象上）；② 绿灯静默、出现即异常；③ 重试是机器的活，不是用户的——**只要还在自动重试就不打断用户**；④ 不用旧结论冒充现状（样本 5 分钟过期、灯色不跨 WiFi 继承）。

## 2. 三个模块一句话

| 模块 | 内容 | PRD |
| --- | --- | --- |
| **A · 常驻入口** | WiFi 列表行内灯色 bars + 控制中心磁贴（三态常显）+ 通知栏**单一网络位**（内容升级不新增卡片：长期建议 > 短期弱网 > 实时状态） | [PRD-1](./PRD-1-常驻入口-WiFi列表与通知.md) |
| **B+A4 · 检测与测速小程序** | TRTC `startSpeedTest` 主路径（备选自研文件测速，UI 全复用）；六态小程序：报告卡（波形逐点刷新动画 / 四指标独立着色 / 3px 灯色外环）+ 全套边界空态 | [PRD-2](./PRD-2-网速测试小程序.md) + 总览模块 B |
| **C · 课中提示** | 存量 3 个阻断界面收敛为**两组件**：底部穿透式抽屉（单行瞬时 ≈2s / 双行驻留=重连重试中，不霸屏中央）+ 全屏中断页（唯一阻断，恢复自动回课）；带宽保护 | [PRD-3](./PRD-3-课中双端提示.md) |

## 3. 关键设计决策（评审已定，开发不要再议）

- **灯色语义**：TRTC quality 1–2 绿 / 3–4 黄 / 5 红（POOR=3 归黄档，消灭现状 3↔4 抖动死区）；输入取 `worst(local, remote)`，红档提示带方向归因（上行差=你的画面 / 下行差=课程画面）；
- **课中零阻断链路**：轻度 = 单行抽屉弹一下即走；重连/重试 = **双行抽屉驻留在底部**（事件行 + 蓝色进度行「第 N 次」外显）；断线给 SDK **30s 自动重连宽限**才升级全屏；全屏页是「课程已暂停」不是「退出」，恢复自动回课，唯一按钮 = WiFi 设置；「重试 / OK」类手动弹窗**全部移除**；
- **抽屉几何（定稿）**：宽 360px（文字区优先，不妥协）、顶角 r50、单行可见高 156 / 双行 190、底边穿透出屏；约束公式「圆角圆心距屏心 + 半径 ≤ 233px」已写进规范标注图；
- **颜色纪律**：红黄绿只属于灯（结论标题 / 超标指标 / bars / 结果页 3px 外环四处）；波形下行灰白 `#F2F5F6`、上行中性淡蓝 `#8FC2F5`；按钮无彩色；
- **数据诚实**：波形点 = 真实回调、按实际到达时刻落位（间隔不均即信息）；四指标 = SDK 最终聚合值（不自行平均）；失败态不给灯色；进度环按 `finishedCount/totalCount` 驱动，绝不按时间。

## 4. 从哪开始（分期）

| 阶段 | 内容 | 说明 |
| --- | --- | --- |
| **C-P0（可立即合入）** | 带宽保护、移除 TRTC 假重试、弱网耗尽跳错页修复、POOR 归黄档、复位重构 | **纯逻辑修改、零新 UI**，见 PRD-3 §6 + §2.3 函数级映射 |
| M1 | 模块 A 常驻入口 + 模块 B 检测调度/测速 | 依赖 TRTC 课外可用性结论（见 §5） |
| M2 | 模块 C 新 UI：抽屉两形态 + 中断页 + HTTP 分级静默重试 | 替换存量 overlay |
| M3 | 上报 / 阈值 OTA / 客服时间线 | 后端并行可先行 |

**开发四步入口**：① 过一遍[开发注意事项清单](./开发注意事项-业务逻辑清单.md)（可勾选约束）→ ② 课中改造对照 PRD-3 §2.3 现状代码函数级映射 → ③ 抄三份参考代码骨架（[检测状态机 C](./参考代码-网络检测状态机.c) / [课中提示状态机 C](./参考代码-课中提示状态机.c) / [后端 FastAPI](./参考代码-后端服务.py)）→ ④ 视觉以[设计规范标注图](./images/界面-设计规范标注.png)与 `Demo-界面交互UI设计.html`（浏览器直接打开，含动效与中英切换）为准。

**参数一律 OTA 可调**（`net_config_t` 下发）：防抖 3 跳、抽屉停留 2s、冷却 300s、每课 ≤2 次、重连宽限 30s、测速硬超时 40s、时效窗 300s、各指标阈值。首发值在 PRD 参数表，调参不发版。

## 5. 待确认阻塞项（需要 owner 回话）

| 事项 | Owner | 阻塞什么 |
| --- | --- | --- |
| TRTC 课外测速可用性实测 | 蔡博 | 模块 B 主路径 or 切自研文件测速（UI 不受影响） |
| PC 教练端文件测速实现细节 | 张恒 | 备选路径服务端对齐 |
| 存量课中弹窗完整清单 | 客户端 | C2 弹窗收敛的保留/删除映射表 |
| 待机整点采样功耗实测 | 硬件 | 60 vs 120 分钟采样间隔取值 |

> 注意：以上不阻塞 C-P0 与全部 UI 开发，只阻塞 B3 测速路径选型与待机采样参数。

## 6. 埋点与验收口径

灯色切换、提示曝光/升级、测速全链路（发起/取消/超时/结果）按总览 PRD 第 8 章埋点表上报；QA 用 PRD-3 §2.1 事件 × 响应矩阵做课中回归；小程序边界用 PRD-2 A4.8 表逐行验收。**验收北极星：一节课内，网络正常时屏上没有任何网络元素；网络出事时，用户不动手，系统自己修好并告知。**
